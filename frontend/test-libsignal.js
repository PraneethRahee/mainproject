import { KeyHelper, SessionBuilder, SessionCipher } from '@privacyresearch/libsignal-protocol-typescript';

async function test() {
  console.log('KeyHelper =', typeof KeyHelper);
  // Just want to check types
}
test();
