# B1: Backend Bootstrap

**Open a new chat and paste this prompt. Implement only this subtask.**

## Task
Bootstrap the Antigravity chat backend. This is the first subtask; there are no dependencies.

## Output
- Express + JavaScript + Socket.IO skeleton
- Health endpoint (e.g. `GET /health` returns 200)
- Socket.IO server attached and listening
- Project structure: `src/`, config, scripts
- `npm run dev` and `npm run build` work

## Done Criteria
- Health endpoint responds
- Socket server accepts connections
- App runs without crashing
- TypeScript compiles

## Stack
Node.js + JavaScript + Express + Socket.IO

## Workspace
Project root: `d:\rahul\chatapp`. Create `backend/` folder for the Node app.

## Reference
See `backend.md` in the project root for full spec.
